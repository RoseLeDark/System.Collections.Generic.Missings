<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>RoseLeDark.Collections.Missings</AssemblyName>
    <Title>RoseLeDark.Collections.Missings</Title>
    <Company>Amber-Sophia Schröck</Company>
    <PackageIcon>SystemEx.png</PackageIcon>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <AssemblyVersion>0.50.0</AssemblyVersion>
    <FileVersion>0.50.0</FileVersion>
    <PackageLicenseFile>LICENSE.md</PackageLicenseFile>
    <Description>RoseLeDark.Collections.Missings — Updated Description
A lightweight extension library providing additional generic collection types and utility structures missing in .NET.
Includes Array&lt;T&gt;, FixedArray&lt;T&gt;, SortedArray&lt;T&gt;, Map&lt;TKey,TValue&gt;, SortedMap&lt;TKey,TValue&gt;, MultiMap&lt;TKey,TValue&gt;, Deque&lt;T&gt;, RingBuffer&lt;T&gt;, and tuple‑like helpers such as Pair&lt;T1,T2&gt; and Triple&lt;T1,T2,T3&gt;.

Provides extended numeric and color types including ColorRGB8, ColorRGB10, ColorRGB16, ColorHDR, ColorXYZ, and a unified Colors utility module.
Adds lightweight hash implementations for non‑cryptographic scenarios, supporting deterministic hashing for engine‑oriented data structures.

Designed as a minimal, allocation‑controlled complement to .NET’s built‑in collections, focusing on predictable behavior, simple APIs, and practical data structures commonly found in other ecosystems.</Description>
    <PackageTags>collections;generic;map;multimap;cache;buffer;datastructures;lowlevel;cache;pair;nodes;multimap;tuple;triple</PackageTags>
    <Copyright>© 2026 KSchattenfeld - Licensed under the EUPL</Copyright>
    <RepositoryUrl>https://github.com/RoseLeDark/System.Collections.Generic.Missings.git</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <Version>0.50.0</Version>
    <PackageProjectUrl>https://roseledark.github.io/SystemEx.Docs/</PackageProjectUrl>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Authors>Amber-Sophia Schröck</Authors>
    <PackageId>RoseLeDark.Collections.Missings</PackageId>
    <SignAssembly>True</SignAssembly>
    <AssemblyOriginatorKeyFile>RoseLeDark.Collections.Missings.snk</AssemblyOriginatorKeyFile>
    <Product>RoseLeDark.Collections.Missings</Product>
    <IncludeSymbols>True</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    <EnforceCodeStyleInBuild>False</EnforceCodeStyleInBuild>
    <AnalysisLevel>latest-recommended</AnalysisLevel>
    <AllowUnsafeBlocks>True</AllowUnsafeBlocks>
    <RootNamespace>SystemEx</RootNamespace>
    <PackageReleaseNotes>## [0.50.00] - 03.07.2026 @Lacking
### Major Documentation Milestone
A significant version jump was made intentionally.  
With this release, the entire public API of SystemEx — including the mathematical canvas
system, layer model, blend modes, hash engines, and all related interfaces — has been fully
documented and formalized.

This marks the completion of approximately half of the planned major‑release scope.
The version number reflects this structural maturity rather than incremental feature changes.

### Added
- Full XML documentation for all canvas interfaces (`ICanvas`, `ISubCanvas`, `ICanvasList`)
- Complete documentation for all hash engines (Groestl, Adler, etc.)
- Formalized mathematical layer model and blend mode semantics
- Unified terminology across the entire SystemEx framework

### Changed
- API surface stabilized and prepared for 1.0
- Internal consistency improvements across all canvas and hash components

### Notes
The jump from `0.12.04` to `0.50.00` is intentional and represents reaching the halfway point
toward the 1.0 major release.

### Codename Update: Ignoring → Lacking
The codename for the 0.x development cycle has been updated from **Ignoring** to **Lacking**.

All SystemEx development codenames follow the theme of “Missing”, reflecting the project’s
purpose of providing features, structures, and utilities that are missing in the .NET ecosystem.

- **Ignoring** represented the early phase where documentation, structure, and formal API
  descriptions were intentionally ignored while core functionality was being built.

- **Lacking** represents the next phase: the system is functionally complete, but still lacking
  documentation, clarity, and formal definitions.

With version **0.50.00**, the documentation milestone has been reached, marking the end of the
“Lacking” phase. All subsequent 0.50.x versions will continue under this codename until the
remaining missing structural components are completed.

</PackageReleaseNotes>
    <AssemblyTitle>RoseLeDark.Collections.Missings</AssemblyTitle>
    <NuGetAuditMode>all</NuGetAuditMode>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
    <Platforms>AnyCPU</Platforms>
    <Configurations>Debug;Release</Configurations>
    <OutputType>Library</OutputType>
    <ApplicationIcon>Logo_256.ico</ApplicationIcon>
    <TargetFramework>net10.0</TargetFramework>
    <RunAnalyzersDuringBuild>False</RunAnalyzersDuringBuild>
    <RunAnalyzersDuringLiveAnalysis>False</RunAnalyzersDuringLiveAnalysis>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <IsAotCompatible>True</IsAotCompatible>
    <Deterministic>False</Deterministic>
    <GenerateAssemblyInfo>False</GenerateAssemblyInfo>
    <WarningsAsErrors>;NU1605</WarningsAsErrors>

  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <IsAotCompatible>True</IsAotCompatible>
    <Deterministic>False</Deterministic>
    <GenerateAssemblyInfo>False</GenerateAssemblyInfo>
    <WarningsAsErrors>;NU1605</WarningsAsErrors>

  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Collections\Generic\Missings\**" />
    <EmbeddedResource Remove="Collections\Generic\Missings\**" />
    <None Remove="Collections\Generic\Missings\**" />
  </ItemGroup>
  <ItemGroup>
	  <None Include="Examples\*.cs" Pack="true" PackagePath="Examples" />
	</ItemGroup>
  <ItemGroup>
    <Compile Remove="Collections\Generic\BasicRope.cs" />
  </ItemGroup>

  <ItemGroup>
    <None Update="CHANGELOG.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="Examples\ExampleHasher.cs">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="Examples\ExampleRamKernelAdd.cs">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Update="SystemEx.png">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
	<None Update="LICENSE.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <EditorConfigFiles Remove=".editorconfig" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="Logo_256.ico" />
  </ItemGroup>

  <ItemGroup>
    <None Include="Collections\Generic\BasicRope.cs" />
  </ItemGroup>

</Project>
